import { add } from "~/lib/basic-math";

const twoPlusTwo = add(2, 2);

// biome-ignore lint/suspicious/noConsoleLog: simple output
// biome-ignore lint/suspicious/noConsole: simple output
console.log(twoPlusTwo);
